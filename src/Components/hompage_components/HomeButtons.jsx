import React from "react";
import Styles from "../../Styles/homepage.css";

const HomeButtons = ({ name }) => {
  return <button className="home_buttons">{name}</button>;
};

export default HomeButtons;
